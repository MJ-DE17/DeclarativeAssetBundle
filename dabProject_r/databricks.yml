bundle:
  name: dabProject_r
  uuid: 5350190c-2918-4fc0-bcd7-a22105ee03fb

include:
  - resources/**/*.yml

variables:
  catalog:
    description: The catalog to use
  schema:
    description: The schema to use

targets:
  dev:
    mode: development
    default: true
    workspace:
      host: https://dbc-627cdb39-0314.cloud.databricks.com   
    variables:
      catalog: dev_catalog
      schema: sales_schema

  prod:
    mode: production
    workspace:
      host: https://adb-7405609984174752.12.azuredatabricks.net
      root_path: /Workspace/Users/manasadevi.j@diggibyte.com/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: prod_catalog
      schema: sales_schema
    permissions:
      - user_name: manasadevi.j@diggibyte.com
        level: CAN_MANAGE
